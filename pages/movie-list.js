import GlobalNav from "../components/GlobalNav";

export default function MovieList() {
  return (
    <main
      style={{
        maxWidth: "86vw",
        marginLeft: "auto",
        marginRight: "auto",
        padding: 30,
      }}
    >
      <GlobalNav />
    </main>
  );
}
